/**
 * Primary booking CTA metrics (reference: ~46px tall, 16px label, ~8px icon–text gap).
 * `leading-none` + `font-medium`: label box matches type for flex centering; weight matches non-bold CTAs.
 */
export const BOOKING_CTA_LAYOUT =
  "h-[46px] min-h-[46px] gap-2 px-5 py-0 text-[1rem] font-medium leading-none tracking-tight shadow-none";

/**
 * Header nav booking CTA: compact height inside 60px rail (oMLX-style), type aligned to nav links.
 */
export const BOOKING_CTA_NAV_LAYOUT =
  "h-9 min-h-9 gap-0 justify-center px-3 py-0 text-[15px] font-medium leading-none tracking-tight shadow-none";
